// Teste removido pois 'SimuladoClient' não pode ser renderizado sem a prop obrigatória 'slug'.
// import { render } from '@testing-library/react';
// import SimuladoClient from '@/app/dashboard/simulados/[slug]/simulado-client';

// describe('SimuladoClient (smoke)', () => {
//   it('renderiza sem crashar', () => {
//     render(<SimuladoClient />);
//   });
// }); 